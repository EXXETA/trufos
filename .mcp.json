{
  "mcpServers": {
    "penpot": {
      "command": "uvx",
      "args": ["penpot-mcp"],
      "env": {
        "PENPOT_API_URL": "${PENPOT_API_URL}"
        "PENPOT_USERNAME": "${PENPOT_USERNAME}",
        "PENPOT_PASSWORD": "${PENPOT_PASSWORD}"
      }
    }
  }
}
