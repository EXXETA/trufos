{
  // Build tooling that runs in Node outside the app itself (Forge, Vite and Vitest configs).
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ESNext"]
  },
  "include": [
    "forge.config.ts",
    "forge.env.d.ts",
    "vitest.config.ts",
    "src/main/vite.config.ts",
    "src/renderer/vite.config.ts",
    "src/renderer/lib/monaco/vite-plugin.ts"
  ]
}
