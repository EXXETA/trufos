import { editor } from 'monaco-editor';

export const REQUEST_MODEL = editor.createModel('');

export const RESPONSE_MODEL = editor.createModel('');

export const SCRIPT_MODEL = editor.createModel('');